# Project

This project has been built to automate the setup steps for new python projects. The project initialises uv, adds a prek.toml file and installs prek and sets up the src & project directories with an empty init file in the project directory.

# Use

The project runs from the setup.py file which can be invoked using the alias pysetup. Create a new directory, navigate into it and run pysetup.
